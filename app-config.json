{
  "command": "npm run start",
  "build_path": ".",
  "stack": "node18",
  "env_variables": {},
  "memory": 256,
  "scripts": {
    "prebuild": "npm install && npx prisma generate && npm run build",
    "preserve": "npm install && npx prisma generate && npm run build"
  }
}
