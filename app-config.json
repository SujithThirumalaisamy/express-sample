{
  "command": "npm run start",
  "build_path": ".",
  "stack": "node18",
  "env_variables": {},
  "memory": 256,
  "scripts": {
    "prebuild": "yarn install && npx prisma generate --generator prodclient && yarn build",
    "preserve": "yarn install && npx prisma generate --generator devclient && yarn build"
  }
}
